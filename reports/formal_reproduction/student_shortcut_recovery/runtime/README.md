# Runtime controls

These are the exact small control/audit files used on the 5090. They are included for code review, not as a portable one-command reproduction package: absolute paths intentionally bind the locked remote layout.

The historical `f739399` suffix remains in several filenames because those controls were first prepared at the A0 commit. Their active embedded S3 identity is the exact clean commit `a0aa4d7ad4b98455e26a2fe6ff2537a321293233`; the only `f739399 -> a0aa4d7` source change is the scalar state-buffer audit fix and its test.

Groups:

- `download_audio_ranges_*`, `run_audio_ranges_*`, `launch_audio_ranges_*`, `query_audio_ranges_*`: validated resumable eight-range download.
- `finalize_timm_cache_*`, `run_finalize_timm_cache_*`: transactional two-file cache verification and receipt.
- `run_s3_worker_*`, `launch_s3_*`, `resume_s3_*`, `query_s3_*`, `preflight_s3_*`: persistent S3 training control and monitoring.
- `query_s3_liveness_*` and `query_s3_metrics_*`: compact process/I/O and scientific-metric monitors.
- `audit_s3_training.py`, `run_s3_training_artifact_audit_*`: full S3 output/config/prediction integrity audit, including a second full hash of both official timm cache files.
- `verify_official_cache_audit_function.py`: isolated execution of the cache-audit function against the locked files.
- `run_s3_posthoc_worker_*`, launch/resume/query/preflight and `audit_s3_posthoc.py`: A0-equivalent diagnostics bound to the audited S3 checkpoint. Model reconstruction is offline, uses the same locked timm cache as training, and re-hashes both official backbone files immediately before evaluation.
- `prepare_s4_candidate_*`, its launch/query pair, and `preflight_s4_uploaded_*`: import the exact S4 bundle into a detached worktree, reuse the locked data/cache junctions, then gate execution on focused tests, `compileall`, the complete repository suite, exact HEAD and a clean worktree.
- `run_s4_worker_*`, launch/resume/query and `audit_s4_training.py`: the single-variable S4 training control. It keeps S0 random initialization and changes only `data.train_augment: true -> false`; the audit binds the candidate/S3 receipts, exact config, three 400-step epochs, ordered T=10 prediction arrays and every required artifact hash.
- `run_s4_posthoc_worker_*`, launch/resume/query/preflight and `audit_s4_posthoc.py`: checkpoint-strict prediction-shortcut and four-way modality-ablation reruns gated on the PASS S4 training audit. The final audit requires training, saved-prediction and checkpoint-rerun AP to agree within `1e-12`.
- `prepare_s7_candidate_*`, `verify_s7_candidate_*`, cleanup and RED-test controls: exact fail-closed construction and verification of the detached S7 candidate. The candidate gate compiled the repository and ran the complete 5090 suite (`477 passed`).
- `prepare_s8_candidate_60100c6.ps1` and `verify_s8_candidate_60100c6.ps1`: construct the exact detached S8 implementation candidate from a SHA-locked bundle, reuse only the nine existing asset junctions, compile project-owned code, run the complete suite, and verify all three S8 audit CLIs.
- `launch_s8_60100c6.ps1`, `run_s8_worker_60100c6.ps1`, `query_s8_60100c6.ps1`, and `resume_s8_60100c6.ps1`: the sole persistent identity+fixed-equal S8 control. It runs exactly `s8_training -> training_audit -> s8_ae -> posthoc_audit`, resumes only completed atomic phases, binds the A–F blocker audit, and explicitly forbids any automatic next experiment, Full run, or canonical-loss change.
- `recover_s8_posthoc_6f39172.ps1`: fail-closed recovery for the isolated real-schema reader bug. It SHA-locks the scientific/audit commits, config, complete bundle, clean 536-test audit receipt, training audit, A–E report and remote-only prediction NPZ; it preserves the failed state and runs only the missing post-hoc auditor. It never starts training or A–E and refuses a duplicate PASS artifact.
- `prepare_s9_candidate_b8ea747.ps1` and `verify_s9_candidate_b8ea747.ps1`: construct and verify the exact detached S9 implementation candidate. They bind the complete bundle, nine existing asset junctions, implementation SHA, clean worktree, project compile, complete 5090 suite (`555 passed`) and the generic training/A–E plus S9 posthoc CLIs.
- `launch_s9_b8ea747.ps1`, `run_s9_worker_b8ea747.ps1`, `query_s9_b8ea747.ps1`, and `resume_s9_b8ea747.ps1`: the sole authorized S8→S9 paper-additive control. It preserves fixed-equal gate and identity temporal path, changes only `student.fusion_mode`, runs exactly `s9_training -> training_audit -> s9_ae -> posthoc_audit`, and never authorizes another experiment or formal Full training.
- `run_s7_worker_*`, launch/resume/query and `audit_s7_training.py`: the single-variable S7 temporal-identity training control. It changes only `student.temporal_path_mode`, preserves temporal-encoder construction/state identity, emits atomic diagnostic checkpoints at steps 400/800/1200, and audits the complete T=10 output chain.
- `diagnose_s7_checkpoint_trajectory.py`, `run_s7_posthoc_worker_*` and its launch/resume/query/preflight controls: checkpoint-strict four-mode inference, path-scale measurement, 100 within-sample shuffles and the pre-approved causal decision at all three checkpoints.
- `audit_s7_posthoc.py`, `test_audit_s7_posthoc.py` and `run_s7_independent_posthoc_audit_*`: an independent recomputation layer that rejects altered deltas, decisions, T=16 inputs, stale source receipts and overwrite attempts. Its five focused tests passed locally and on the 5090.
- `prepare/verify/preflight/run/launch/query/resume_zero_training_*`: exact c181ffb A–F control chain. It binds nine resource junctions, S7 and canonical Full source hashes, the verified Python/Git/GPU environment and atomic `ae→f→audit` phases; the launch receipt explicitly records `starts_training=false` and `starts_s8=false`.

The zero-training candidate passed project-boundary compileall and the complete suite (`526 passed in 346.28s`). Its persistent worker completed A–F and the independent audit with exit 0 and empty stderr for every phase. The S8 scientific candidate then passed compileall and the complete suite (`536 passed in 346.15s`). Its post-hoc reader-fix candidate independently passed compileall and `536 passed in 347.58s`; the recovered post-hoc audit is PASS with empty stderr. The 17-mode NPZ and raw logs remain remote; only audited JSON receipts are published.

The S9 exact candidate passed compileall and the complete suite (`555 passed in 372.02s`). Its persistent worker completed training, training-audit, A–E and posthoc with exit code 0 and empty stderr. The independent re-audit repeated the posthoc reader against the same remote-only NPZ and produced the identical SHA; the scientific outcome is the pre-registered `FAIL`, while artifact integrity is PASS. Checkpoints, NPZ, data, caches and logs remain remote.

PowerShell files were parsed locally and again after upload. Python files passed local Ruff and `py_compile`; the exact uploaded Python hashes also passed remote `py_compile`. The locked remote environment does not contain Ruff, so no remote Ruff success is claimed. The S7 posthoc launcher was independently re-reviewed before launch; its phase-acceptance race was corrected and re-parsed before the successful persistent worker started.
